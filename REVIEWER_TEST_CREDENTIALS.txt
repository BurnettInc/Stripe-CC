REVIEWER TEST CREDENTIALS — CollectionsCopilot (Stripe App marketplace listing → Test credentials)

Test-mode install → Settings → Subscribe produces a $0 Checkout automatically
(NO coupon entry needed): Stripe's test-mode install link stores oauth_tokens.livemode=0,
and the backend pre-attaches the 100%-off promo server-side for test-mode installs.

Fallback (explicit reviewer path): open
  https://stripe-cc-production.up.railway.app/billing/checkout?tier=standard&promo=REVIEWER100
(or tier=pro) from the signed-in dashboard to get a $0 Checkout Session directly.

Promo code REVIEWER100 — 100% off, duration=forever, created 2026-08-17 on the live billing
account acct_1TvKTAAD4cJGS9Cr (promo_1U5PYjAD4cJGS9Cro4A2TdbI). Standard and Pro are both
fully accessible at $0. Complete checkout with test card 4242 4242 4242 4242 (any future
expiry, any CVC) — no card is charged.

Real (live-install) customers are NEVER discounted: only the two reviewer signals above
attach the promo. Every other checkout keeps allow_promotion_codes=true and charges the
published $15/$29 price.
